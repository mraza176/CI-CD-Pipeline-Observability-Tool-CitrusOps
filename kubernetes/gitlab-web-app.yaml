apiVersion: v1
kind: Service
metadata:
  name: gitlab-web-app
  namespace: citrusops
spec:
  selector:
    app: gitlab-web-app
  ports:
    - port: 8081
      targetPort: 8081
  type: ClusterIP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gitlab-web-app
  namespace: citrusops
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gitlab-web-app
  template:
    metadata:
      labels:
        app: gitlab-web-app
    spec:
      containers:
        - name: gitlab-web-app
          image: citrusops-gitlab-web-app:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8081
          env:
            - name: MONGODB_URI
              value: "mongodb://mongouser:mongopassword@mongo:27017"
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 10
            periodSeconds: 5
